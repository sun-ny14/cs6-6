# 보안 및 등교 암호 수정 배포 안내

## 변경 내용

- 교사가 수동 저장한 등교 암호는 자정이 지나도 다시 변경할 때까지 유지됩니다.
- 학생 브라우저는 비공개 `settings/password`를 읽지 않고 서버 함수에서 암호를 검증합니다.
- 학생 직접 등교, 포인트 상점 구매, 용사의 방 구매, 주문 사용·승인·거절을 서버 함수에서 검증합니다.
- 포인트 상점은 서버에 저장된 가격·재고·구매 한도를 사용하며 하나의 트랜잭션으로 처리합니다.
- 학생은 자신의 포인트, 역할, 상점 역할 여부, 방 코인 및 인벤토리를 직접 수정할 수 없습니다.
- 일반 학생에게는 전체 사용자 레코드 대신 방과 공개 프로필만 포함한 `publicProfiles`가 제공됩니다.
- 성적, 예산, 전체 출결 로그, 교사 설정과 이메일 매핑은 교사만 읽을 수 있습니다.
- 청소 역할과 상점 역할은 서버가 만든 `access/{uid}` 정보로 판정합니다.
- 상점 출력값을 HTML 이스케이프하여 기존 악성 데이터에 의한 저장형 XSS를 차단합니다.

## 배포 순서

새 웹 코드는 Cloud Functions를 로그인 과정에서 사용하므로 반드시 아래 순서를 지켜 주세요.

1. Node.js 22 및 Firebase CLI를 준비합니다.
2. 프로젝트 루트에서 `firebase login` 및 `firebase use`로 `cs6-6class` 프로젝트가 선택됐는지 확인합니다.
3. `functions` 폴더에서 `npm ci`를 실행합니다.
4. 프로젝트 루트에서 먼저 `firebase deploy --only functions`를 실행합니다.
5. 함수 배포가 성공한 뒤 `firebase deploy --only database`로 규칙을 배포합니다.
6. 마지막으로 웹 파일을 기존 호스팅 방식으로 업로드합니다.
7. 교사 계정으로 한 번 로그인합니다. 기존 사용자 공개 프로필과 주문별 개인 보기가 자동 생성됩니다.
8. 일반 학생, 청소 역할, 상점 역할 계정으로 각각 로그인하여 메뉴와 구매·등교를 확인합니다.

함수보다 웹 파일 또는 데이터베이스 규칙을 먼저 배포하면 로그인이나 구매가 일시적으로 실패할 수 있습니다.

## 배포 후 확인

- 교사 설정에서 4자리 암호를 저장하고 전자칠판에 즉시 표시되는지 확인합니다.
- 다음 날에도 같은 암호가 전자칠판에 표시되고 학생 등교에 사용되는지 확인합니다.
- 일반 학생 계정에서 개발자 도구를 사용해 `users`, `settings`, `grades`, `budgetRecords` 전체 읽기가 거부되는지 확인합니다.
- 학생이 자신의 `points`, `role`, `isHelper`, `roomCoins`, `housingInventory`를 직접 수정할 때 거부되는지 확인합니다.
- 동시에 여러 번 구매해도 포인트·재고·한도가 한 트랜잭션으로 정확히 반영되는지 확인합니다.
- 상점 역할은 주문 처리만, 청소 역할은 청소 확인만 가능하며 일반 학생에게 해당 경로가 거부되는지 확인합니다.

## 참고

전자칠판 페이지는 로그인 없이 표시되므로 `blackboardDisplay` 경로의 표시용 암호는 공개 정보입니다. 교실 전자칠판 전용 네트워크에서만 암호를 보여야 한다면 별도의 로그인 또는 네트워크 접근 제한이 추가로 필요합니다.
