# 다른 기기에서 주소만 열어 전자칠판 보기

## 적용 순서

1. 압축파일의 파일들을 기존 GitHub 프로젝트에 같은 폴더 구조로 덮어쓰고 GitHub Pages 배포가 끝날 때까지 기다립니다. 이전 방 기능과 전자칠판 자동 전환 수정도 포함되어 있습니다.
2. Firebase 콘솔 → Realtime Database → 규칙에서 `database.rules.json` 내용을 붙여넣고 **게시**합니다. GitHub에 JSON 파일을 올리는 것만으로 Firebase 규칙이 바뀌지는 않습니다.
3. 관리자 PC에서 새 버전 학급 앱에 로그인한 뒤 전자칠판 관리 탭의 **전자칠판 공유 완료** 문구를 확인합니다. 처음 한 번 서버에 표시용 자료를 만드는 과정입니다.
4. 실제 전자칠판이나 다른 기기에서 아래 주소를 엽니다. 보는 기기는 로그인할 필요가 없습니다.

https://sun-ny14.github.io/cs6-6/blackboard.html

이미 열어둔 페이지는 적용 후 한 번 새로고침합니다. 이후 공유 데이터 변경은 실시간으로 수신합니다.

## 규칙

```json
{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null",
    "pointLogs": {
      ".indexOn": ["name"]
    },
    "blackboardDisplay": {
      ".read": true
    }
  }
}
```

`blackboardDisplay`만 로그인 없이 읽을 수 있습니다. 비로그인 사용자의 쓰기는 허용하지 않습니다. 기존 로그인 계정의 전체 읽기·쓰기 권한은 이번 변경에서 그대로 유지합니다.

## 공개되는 범위

- 전자칠판용 시간표, 공지, 배움공책·메모, 교사 전달사항
- 화면에 필요한 학생 이름·번호·좌석, 오늘 등교 여부·청소 확인 여부, 필수 과제·완료 여부
- 이메일, 계정 식별자, 포인트·경험치, 방 정보, 출결 사유·증빙 여부·벌점, 이전 날짜의 출결·청소 기록은 복사하지 않습니다.

주소만으로 공개 열람하는 방식이므로, 학생 이름과 화면에 표시되는 상태는 주소를 아는 사람도 볼 수 있습니다. 로그인 없이 화면 전체를 공유하면서 그 화면의 학생 정보까지 비공개로 숨길 수는 없습니다.

## 동기화 동작과 제한

- 새 버전의 로그인한 학급 앱(관리자 또는 학생)이나 로그인한 전자칠판이 서버의 변경을 받아, 필요한 필드만 표시용 경로로 자동 복사합니다.
- 관리자뿐 아니라 학생 앱에서도 공유를 갱신하므로, 학생의 출결·청소 변경도 앱 사용 중에는 반영됩니다. 데이터를 복사할 때 이메일·포인트 등 원본의 다른 필드는 제외합니다.
- 공유한 자료는 Firebase에 저장되므로 관리자 창을 닫아도 다른 기기에서 마지막 공유 내용을 볼 수 있고, 저장된 시간표에 따른 자동 전환도 계속됩니다.
- 로그인한 새 버전 앱이 하나도 열려 있지 않은 상태에서 Firebase 콘솔이나 다른 구버전 앱이 원본 자료를 바꾸면, 다음에 새 버전 앱을 열 때 공유 자료가 갱신됩니다. 이러한 외부 변경까지 상시 반영하려면 별도의 서버 동기화 기능이 필요합니다.
- 여러 기기의 저장된 내용이 공유됩니다. 시간표를 눌러 수동 미리보기를 선택하는 조작은 각 화면에만 적용되며 다음 시간 구간에 자동 복귀합니다.

## 검증

`node --test tests/*.test.cjs`로 실행합니다. 로그인하지 않은 두 화면의 갱신, 공개 데이터 필드 제한, 최초 공유 대기, 저장 실패·재시도, 날짜 경계와 기존 방 기능을 모의 환경에서 검증합니다. 실제 Firebase 규칙 게시나 운영 데이터 쓰기는 수행하지 않았습니다.
