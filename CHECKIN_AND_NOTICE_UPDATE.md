# 등교 암호·공지 달력 통합 수정본

이 수정본에는 포인트/경험치 입력칸 수정도 함께 포함되어 있습니다.

## 적용

관리자용·학생용 두 창 자동 실행 복구 방법은 `TWO_WINDOWS_SETUP.md`를 먼저 참고하세요. 이번 배포 ZIP에는 PC별 `.vscode/settings.json`을 포함하지 않습니다.

1. 기존 소스 폴더를 백업한 뒤, 압축 안의 소스를 같은 경로에 덮어쓰세요.
2. 기존 방식으로 웹 파일을 업로드하세요. 새 `functions/checkin-password-core.js`와 `js/checkin-password.js`도 웹에서 열릴 수 있어야 합니다.
3. 교사 화면과 전자칠판 화면을 모두 닫았다가 다시 열고 Ctrl+F5로 새로고침하세요.
4. 교사 계정으로 학급 앱에 한 번 로그인하세요. 최초 적용 때 오늘의 새 암호를 생성하고 전자칠판과 공유합니다.

웹 파일만 적용하면 로그인한 앱이 켜져 있을 때 자정에 자동 변경합니다. 절전·오프라인으로 놓쳤다면 다시 연결하거나 등교를 제출할 때 먼저 오늘의 암호로 갱신합니다. 모든 앱을 꺼둔 상태에서도 자정에 변경하려면 아래 서버 예약 작업을 한 번 배포하세요.

## 모든 기기가 꺼져 있어도 자동 변경하기

Firebase 프로젝트 `cs6-6class`에 Cloud Functions를 배포해야 합니다. 프로젝트의 Blaze 요금제와 배포 권한이 필요합니다. 이 압축 파일을 받는 것만으로 서버 기능이 배포되거나 과금 설정이 변경되지는 않습니다.

Node.js 22 환경에서 압축을 푼 프로젝트 폴더를 열고 실행하세요.

```sh
npm install -g firebase-tools
firebase login
npm ci --prefix functions
firebase deploy --project cs6-6class --only functions:rotateDailyCheckinPassword,functions:syncCheckinPasswordDisplay
```

포함된 `firebase.json`은 새 기능 전용 `checkin-password` 코드베이스를 사용합니다. 기존 Firebase 설정 파일이 별도로 있다면 기존 설정을 보존하면서 functions 항목을 병합하세요. 기존 데이터베이스 규칙을 변경할 필요는 없습니다. 위 명령은 데이터베이스 규칙이나 웹 호스팅을 배포하지 않습니다.

- `rotateDailyCheckinPassword`: 매일 00:00, `Asia/Seoul` 기준 예약 실행.
- `syncCheckinPasswordDisplay`: 암호가 변경되면 전자칠판의 암호 항목만 갱신.
- Cloud Scheduler 실행 및 네트워크에는 지연이 있을 수 있습니다. 날짜가 지난 암호는 전자칠판에서 숨기고, 등교 제출 전에도 오늘 날짜인지 확인합니다.
- 서버 배포는 이 작업 환경에서 실행하지 않았습니다.

공식 문서:
- https://firebase.google.com/docs/functions/schedule-functions
- https://firebase.google.com/docs/functions/manage-functions

## 사용 방법

### 등교 암호

- 매일 한국시간 자정을 기준으로 새로운 네 자리 숫자를 생성합니다. 전날 암호와 같은 숫자는 뽑지 않습니다.
- 기존 **난수 비번 생성 → 시스템 저장** 버튼 사용 방식은 유지됩니다. 직접 네 자리 숫자를 입력해 저장할 수도 있습니다.
- 수동으로 저장한 암호는 그날 유지되고 다음 자정에 자동 변경됩니다.
- 지각 시각 등 다른 설정만 저장하면, 오래 열린 설정창의 이전 암호가 새 암호를 덮어쓰지 않습니다.
- 전자칠판은 설정된 ‘아침’ 화면에 오늘의 암호를 크게 표시합니다. 다른 교시 화면에는 표시하지 않습니다.
- 여러 기기에서 동시에 변경을 시도해도 Firebase 트랜잭션과 변경 번호를 사용하여 이미 적용된 당일 암호를 다시 자동 변경하지 않습니다.

### 공지 달력

- 전자칠판 관리의 ‘공지 달력’에서 한 달 전체를 볼 수 있습니다.
- 날짜별 공지 요약, 오늘 날짜, 선택한 날짜를 표시합니다.
- 이전 달·다음 달·오늘 버튼 또는 날짜 입력으로 이동합니다.
- 날짜를 누르고 공지를 작성한 다음 **공지 저장**을 누르세요.
- 저장하지 않은 글은 같은 페이지에서 날짜를 옮겨도 임시로 보관합니다. 새로고침하거나 창을 닫으면 임시 글은 사라지므로 작성 후 저장하세요.

### 하교 화면

- 상단에 **내일 공지**와 실제 날짜를 표시합니다.
- ‘내일’은 한국시간 기준 다음 달력 날짜입니다. 금요일의 내일은 토요일이며 다음 등교일로 건너뛰지 않습니다.
- 내일 공지가 없으면 ‘등록된 내일 공지가 없습니다.’라고 표시합니다.
- 기존 청소·과제 미완료 및 교사 전달사항은 유지됩니다.

## Firebase 다운로드 사용량

- 매분 로컬 시계로 날짜를 확인하지만, 당일 암호가 정상 반영된 상태에서는 추가 DB 읽기·쓰기를 하지 않습니다.
- 시계의 초 단위 화면 갱신도 서버 조회를 발생시키지 않습니다.
- 날짜 전환, 최초 연결, 수동 변경, 실패 후 재시도 등에만 암호 확인·공유 작업이 필요합니다.
- 공개 전자칠판에는 `blackboardDisplay/checkinPassword`의 암호·날짜·변경 번호·수정 시각만 추가합니다. 이 경로는 기존 전자칠판처럼 로그인 없이 읽을 수 있으므로 등교 암호를 보여 달라는 요청에 따라 의도적으로 공개됩니다.
- 일반 전자칠판 공유가 별도의 암호 항목을 덮어쓰지 않도록 변경했습니다.
- 공지 달력은 이미 불러온 공지 자료로 그립니다. 날짜나 월을 클릭할 때마다 다시 다운로드하지 않습니다.
- 학생 수·공지량·접속 기기 수와 기존 전자칠판 동기화에 따라 전체 앱 사용량은 달라집니다. 추가 기능 자체의 전송 데이터와 전체 앱 전송량은 구분해서 봐야 합니다.

## 검증

```sh
node --test tests/checkin-password.test.cjs tests/checkin-settings.test.cjs tests/notice-calendar.test.cjs tests/blackboard.test.cjs tests/blackboard-share.test.cjs
```

41개 테스트 통과: 한국시간 자정, 동시 변경 재시도, 수동 암호 유지, 이전 날짜 폼 저장, 서버 시간 보정, 미접속 기기, 일반 상태 1,440회 확인 시 추가 DB 읽기·쓰기 0회, 아침 표시, 연말/월말 내일 공지, 윤년 달력, 임시 작성 내용 유지, 느린 저장·실패 후 재시도, 기존 전자칠판 동작.

스크립트 문법과 로컬 파일 경로도 확인했습니다. Firebase 운영 서버의 배포·실제 과금량·실제 브라우저 화면은 검증하지 않았습니다. 브라우저 시각 확인은 이 작업 환경의 로컬 미리보기 접근 제한 때문에 진행하지 못했습니다.
