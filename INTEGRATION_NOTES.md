# 코드 통합 내역

## 2026-09-02 학생 전용 창과 읽기 전용 연결 진단

- 학생 창을 별도 Chrome 사용자 데이터 디렉터리로 실행하는 `open-student.cmd`를 추가했습니다. 기존 교사 프로필을 유지하고 학생은 첫 실행 시 학생 계정으로 로그인합니다.
- 두 창 실행 파일에서 대기 명령을 제거하고 학생 전용 실행 모드를 추가했습니다. 이 변경은 학생 권한을 자동으로 부여하지 않습니다.
- `connection-check.html`과 `js/connection-check.js`로 SDK 초기화·HTTPS 응답·실시간 연결·로그인·설정 읽기를 나누어 표시합니다. 실제 이름·이메일·토큰을 표시하지 않으며 DB 쓰기를 수행하지 않습니다.
- 이번 보완 ZIP은 PC별 `.vscode/settings.json`을 덮어쓰지 않습니다. 교실 네트워크의 접속 실패 원인은 아직 확정하지 않았으며, 실제 PC에서 진단 결과 확인이 필요합니다.

## 2026-09-02 Go Live 교사·학생 두 창 복구

- 앞선 Go Live 수정에서 사용자 지정 실행 명령을 해제해 한 창만 열리던 변경을 복구합니다. `setup-two-windows.cmd`를 한 번 실행하면 현재 프로젝트의 두 창 실행 파일을 Live Server에 연결합니다.
- 기존 교사용 Chrome `Profile 5`, 학생용 `Default`를 별도 창으로 실행합니다. 앱 로그인과 역할 판정은 기존대로 유지합니다.
- Chrome 설치 경로 3곳을 확인하고, Go Live가 전달한 실제 서버 포트를 유지하면서 두 창 모두 `index.html`을 엽니다.
- 프로젝트 경로를 따옴표로 감싸 공백이 있는 경로를 처리합니다. 환경마다 실제 프로젝트 경로가 다르므로 ZIP을 기존 폴더에 덮어쓴 뒤 설정 파일을 한 번 실행해야 합니다. 자세한 순서는 `TWO_WINDOWS_SETUP.md`에 있습니다.
- Windows 실행 환경이 없어 실제 창 실행은 확인하지 못했습니다. 앱 코드는 이번 변경에 포함하지 않았습니다.

## 2026-09-02 구매 중 Firebase 연결 끊김 대응

- 사용자 콘솔의 `Error: disconnect`와 `cancelSentTransactions_` 호출을 확인했습니다. Go Live 주소 때문이라는 의미가 아니라 Firebase 연결이 저장 도중 끊긴 상황입니다. 연결이 끊긴 네트워크상의 원인은 이 로그만으로 확정할 수 없습니다.
- 구매 전에 Firebase의 `.info/connected`로 실제 DB 연결을 확인합니다. 연결 대기는 최대 12초이며, 연결 끊김 오류에만 같은 구매 번호로 최대 3회까지 저장을 시도합니다. 권한 오류는 재시도하지 않고 별도로 안내합니다.
- 완료 응답만 유실된 경우 기존 구매 내역을 확인하여 추가 차감 없이 완료 처리합니다. 미확정 구매 번호는 현재 탭과, 사용 가능한 경우 `sessionStorage`에 보관해 새로고침 후 같은 물건을 눌러 확인해도 재사용합니다. 브라우저 저장소가 차단되었거나 탭을 닫은 경우에는 구매 내역을 먼저 확인하세요.
- 연결 실패 후에는 이전 물건의 구매 확인을 먼저 마친 뒤 다른 물건을 살 수 있습니다. 확정된 구매 건의 번호는 정리해 이후 별도 구매를 허용합니다. 교사는 계속 무료입니다.
- 네트워크 차단이나 지속적인 Firebase 접속 실패 자체를 해소하는 변경은 아닙니다. 연결이 복구되어야 서버에 저장됩니다.
- 근거: [Firebase 연결 상태 감지](https://firebase.google.com/docs/database/web/offline-capabilities#section-connection-state).
- 모의 DB 검증: 저장 전 끊김, 서버 저장 후 응답 유실, 연결 대기 종료, 새로고침 후 동일 건 확인, 재연결 중 방 이동, 권한 오류 분리를 포함합니다. 실제 교실 네트워크는 확인하지 않았습니다.

## 2026-09-02 하우징 구매 저장·교사 프로필

- 학생 구매는 기존 하우징 전용 코인 `roomCoins`(C)을 사용합니다. 일반 포인트 `points`(P)는 차감하지 않습니다. **교사는 무료 구매를 유지합니다.**
- 코인 차감, `housingInventory` 보관함, `housingPurchases` 구매 내역을 사용자 경로의 한 트랜잭션으로 저장합니다. 서버가 보관함과 구매 내역 저장을 확인한 뒤 완료를 표시합니다.
- 학생용 사용자 자료가 없는 교사 계정도 자신의 하우징 자료를 초기화하여 무료 구매가 저장됩니다. 없는 학생 자료를 새로 만들거나 저장 없이 완료로 표시하지 않습니다.
- 방/상점 진입 시 코인 보상 동기화도 트랜잭션으로 처리합니다. 보상이 바뀌지 않으면 쓰지 않아 구매 잔액을 덮어쓰거나 구매 트랜잭션을 취소하던 경로를 없앴습니다.
- 내 방에 구매 내역 확인 버튼을 추가했습니다. 새 버전 구매부터 물건·사용 코인·구매 후 잔액·한국 시간 구매일이 표시됩니다. 이전 구매 물건은 기존 보관함에 유지됩니다. 교사 구매는 무료로 기록됩니다.
- 교사 프로필은 긴 포인트 내역이 창 전체 높이를 늘리지 않도록 목록을 별도로 스크롤합니다. 작은 화면에서는 창 전체를 스크롤할 수 있습니다.
- 교사가 학생 프로필의 ‘학생 방 방문하기’를 누르면 상세창을 닫고 해당 학생 방을 엽니다. 이전 프로필 진입 함수도 같은 화면으로 연결했습니다.
- 교사는 학생 방에서 이모지 반응과 방명록 확인을 할 수 있습니다. 학생과 동일하게 한 학생에게 한국 시간 하루 한 번 반응을 남깁니다. 학생 방에서는 구매·꾸미기·학생 구매 내역 버튼을 표시하지 않습니다.
- 적용: 이 누적 수정 ZIP의 폴더 구조를 유지한 채 기존 프로젝트에 덮어쓴 후 새로고침합니다. Go Live는 중지 후 다시 시작하세요. GitHub Pages에도 적용하려면 사용자가 수정 파일을 저장소에 업로드해야 합니다. 전자칠판 공유 규칙 적용은 `BLACKBOARD_SETUP.md`를 따릅니다.
- 검증: `node --test tests/*.test.cjs`의 52개 자동 검증과 전체 JavaScript 문법 검사를 통과했습니다. 구매·교사 반응·프로필 동작은 모의 DB/DOM을 사용했습니다. 실제 Firebase 계정에 쓰지 않았으며, 브라우저 보안 정책으로 로컬 미리보기 접근이 차단되어 화면 배치는 실제 브라우저에서 확인하지 못했습니다.

## 2026-09-02 Go Live 실행 설정

- `.vscode/settings.json`에서 특정 PC의 절대 경로에 의존하던 사용자 지정 실행 명령을 해제했습니다.
- Go Live가 프로젝트 루트를 제공하고 Chrome에서 화면을 열도록 설정했습니다. 기존 두 프로필 실행 스크립트는 파일로 보관하되 Go Live에서 자동 호출하지 않습니다.
- 설정을 적용한 뒤 Live Server를 중지하고 다시 시작합니다. 자동 브라우저 실행이 계속 실패하면 Chrome에서 `http://127.0.0.1:5500/index.html` 또는 `http://127.0.0.1:5500/blackboard.html`을 직접 엽니다.
- JSON 형식과 실행 설정값을 검증했습니다. 실제 사용자 Windows에서의 브라우저 실행은 별도 확인이 필요합니다.

## 2026-09-02 기기 간 전자칠판 공유

- 로그인하지 않은 전자칠판은 `blackboardDisplay`만 실시간으로 읽습니다. 원본 사용자·포인트·출결 기록에는 접근하지 않습니다.
- 새 버전 로그인 앱은 `js/blackboard-share.js`로 화면 표시 필드만 골라 공유 경로를 갱신합니다.
- 기본 시간표를 실제 저장된 내용처럼 표시하던 오류를 막고, 초기 공유 대기·읽기 거부 안내를 추가했습니다.
- `database.rules.json`을 Firebase 규칙에 게시해야 합니다. 자세한 적용 순서와 공개 범위·동기화 제한은 `BLACKBOARD_SETUP.md`에 있습니다.

## 2026-09-02 전자칠판 자동 전환

- 메모 입력칸에 포커스가 있으면 화면 갱신을 무조건 중단하던 동작을 수정했습니다. 같은 교시에서는 입력 상태를 유지하고, 교시·쉬는 시간·날짜가 바뀌면 메모 저장을 요청한 뒤 해당 시간 화면으로 전환합니다.
- 시간표를 클릭한 수동 미리보기는 다음 시간 구간에서 자동 모드로 복귀합니다. 즉시 복귀 버튼도 유지합니다.
- 날짜와 주가 바뀌어도 작성하던 메모는 원래 날짜와 교시에 저장합니다.
- 창으로 돌아오거나 절전에서 복귀하면 현재 시간에 맞게 화면을 갱신합니다.
- `node --test tests/blackboard.test.cjs`의 11개 시나리오로 교시 전환, 메모 포커스, 수동 복귀, 날짜 경계, 사용자 지정 시간표를 검증했습니다. 실제 교실 계정과 기기의 브라우저 검증은 포함하지 않습니다.

## 2026-09-02 친구 방·이모지 방명록

- 친구 카드 클릭을 실제 방 렌더러에 연결하고 `이름의 방`을 표시합니다.
- 방 탭을 직접 열면 내 방으로 돌아옵니다. 내 방에서만 상점, 보관함, 배경 선택 및 가구 편집이 가능합니다.
- 친구 방에는 이모지 반응과 방명록 확인만 표시합니다. 내 방에서도 받은 반응을 확인할 수 있습니다.
- 친구별·방문자별 하루 한 번만 반응을 남깁니다. 한국 시간 날짜를 사용하고 서버 시간 차이를 보정합니다.
- 일일 반응 기록과 방명록을 같은 Realtime Database 트랜잭션에 저장합니다. 기존 방·가구·방명록 데이터는 보존합니다.
- 학생과 관리자 모두 상점 내부의 닫기 버튼을 이용할 수 있습니다.
- `node --test tests/housing.test.cjs`로 방 전환, 권한 분리, 동시 반응, 날짜 경계, 저장 실패와 기존 기록 호환성을 검증합니다. 테스트는 모의 데이터베이스를 사용하며 실제 Firebase 권한과 브라우저 화면 확인은 포함하지 않습니다.

## 주요 수정

- 전역 상태를 `window` 기준으로 통합
- 등교 암호 확인, 정상/지각 판정, 출결 저장 기능 연결
- 출결 새로고침과 날짜별 요약/로그 기능 연결
- 저장된 좌석 배치를 출결 및 청소 화면과 공유
- 학급관리 라우터 중복 제거 및 실제 HTML 컨테이너 연결
- 포인트 환불 함수명 충돌 제거
- 포인트 증감 공통 함수 및 로그 형식 통합
- 아바타 선택 기능 연결
- 전자칠판 Firebase 로딩 및 `checkins` 데이터 구조 통합
- 사용되지 않던 중복 용사 렌더링 파일 `js/main.js` 제거

## 확인 결과

- 모든 JavaScript 파일 `node --check` 통과
- HTML 인라인 이벤트에서 호출하는 주요 함수 연결 확인
- Git whitespace 검사 통과

## 배포 전 확인

- Firebase Realtime Database Rules에서 관리자 전용 쓰기 경로를 제한해야 합니다.
- 실제 Firebase 계정으로 로그인하여 등교, 좌석, 포인트, 학급관리, 전자칠판을 순서대로 확인하세요.
